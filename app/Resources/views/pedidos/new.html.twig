{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo producto</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('productos_index') }}">Volver</a>
        </li>
    </ul>
{% endblock %}
